# .bashrc

if [ -f /etc/bashrc ]
then
	. /etc/bashrc
fi

alias gst='git status'
alias gcm='git commit -m'
alias gco='git checkout'
alias gb='git branch'
alias sc='ssh centos'
alias vi='vim -O'
alias b='run bootstrap'
